#include <physical_layouts.dtsi>

/ {
  chosen {
      zmk,physical-layout = &physical_layout0;
  };

  physical_layout0: physical_layout_0 {
      transform = <&default_transform>;
      compatible = "zmk,physical-layout";
      display-name = "Redsplit Keyboard";
  };
};
